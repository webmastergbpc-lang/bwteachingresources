---
product: "R2 Bucket Manager"
version: "3.5.0"
date: "2026-03-15"
slug: "v3-5-0"
docker_image: "writenotenow/r2-bucket-manager"
github_repo: "neverinfamous/R2-Manager-Worker"
description: "Migrates to lightningcss for 15x faster builds, refreshes core dependencies, and applies critical undici security patches."
---

# R2 Bucket Manager v3.5.0

## Highlights

- **LightningCSS Migration**: Switched Vite's CSS transformer to `lightningcss`, drastically reducing build times from 29s to 1.9s.
- **Dependency Refresh**: Synchronized the ecosystem bumping `workers-types`, `vite`, `wrangler`, and `esbuild` to lock in bug fixes and better types.

## Changed

- **Dependencies**: Bumped `@cloudflare/workers-types`, `@types/node`, `@vitejs/plugin-react`, `esbuild`, `vite`, and `wrangler` to latest versions.
- **Build Tooling**: Added `lightningcss` to devDependencies for optimized build speeds.
- **Performance**: Migrated CSS transformer and minifier to `lightningcss` in Vite 8, accelerating application compile time from 29 seconds to 1.9 seconds.

## Security

- **undici**: Fixed multiple high severity vulnerabilities in `undici` by pinning exact patched version `7.24.4` via package.json overrides.

## Install

```bash
docker pull writenotenow/r2-bucket-manager:v3.5.0
```

[Full Changelog](https://github.com/neverinfamous/R2-Manager-Worker/compare/v3.4.4...v3.5.0)
